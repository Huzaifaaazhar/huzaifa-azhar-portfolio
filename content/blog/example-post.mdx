---
title: "Example post — replace or delete me"
description: "A placeholder post proving the MDX pipeline (frontmatter, rendering, RSS, JSON-LD) works end to end. Not real content."
date: "2026-07-16"
tags: ["example"]
draft: true
---

This file exists to prove the blog pipeline works — frontmatter parsing,
MDX rendering, the `/blog` listing, `/blog/[slug]`, RSS, and per-post
JSON-LD all read from files exactly like this one.

It ships with `draft: true`, so it is excluded from the listing, the
sitemap, and the RSS feed. Delete this file (or write over it) once you
have real posts, or flip `draft: false` on a copy to see how a live post
looks.

## Regular MDX just works

Headings, lists, `inline code`, and **bold/_italic_** text all render
normally — this is standard Markdown/MDX, no custom components required
to write a first post.
